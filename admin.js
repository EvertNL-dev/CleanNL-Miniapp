const adminState={categories:[{name:'Frozen Cleansift',products:[{title:'Emergency',description:'Korte omschrijving',image:'',brand:'TopOrganic',variants:[{amount:'10 gram',price:'25'},{amount:'20 gram',price:'45'},{amount:'50 gram',price:'100'}]}]},{name:'Static Cleansift',products:[]}]};function createVariantItem(data={amount:'',price:''}){const row=document.createElement('div');row.className='variant-item';row.innerHTML=`<input type="text" placeholder="Hoeveelheid, bv. 10 gram" value="${data.amount}"><input type="text" placeholder="Prijs, bv. 25" value="${data.price}"><button type="button" class="btn-danger">X</button>`;row.querySelector('button').addEventListener('click',()=>row.remove());return row}function createEditor(product={title:'',description:'',image:'',brand:'',variants:[{amount:'',price:''}]}){const box=document.createElement('div');box.className='editor';box.innerHTML=`<div class="row-2"><div class="field"><input type="text" placeholder="Nieuw product" value="${product.title}"></div><div class="field"><input type="text" placeholder="Merk / Farm" value="${product.brand}"></div></div><div class="field"><textarea placeholder="Beschrijving">${product.description}</textarea></div><div class="field"><input type="text" placeholder="Afbeelding URL" value="${product.image}"></div><div class="variant-box"><h4>Hoeveelheden en prijzen</h4><div class="variant-list"></div><div class="actions" style="margin-top:10px"><button type="button" class="btn-secondary add-variant">+ Hoeveelheid toevoegen</button></div></div><div class="actions" style="margin-top:12px"><button type="button" class="btn-danger remove-product">Product verwijderen</button></div>`;const list=box.querySelector('.variant-list');(product.variants||[]).forEach(v=>list.appendChild(createVariantItem(v)));box.querySelector('.add-variant').addEventListener('click',()=>list.appendChild(createVariantItem()));box.querySelector('.remove-product').addEventListener('click',()=>box.remove());return box}function renderAdmin(){const root=document.getElementById('categories');root.innerHTML='';adminState.categories.forEach(category=>{const section=document.createElement('section');section.className='category';section.innerHTML=`<div class="category-head"><h2>${category.name}</h2><button type="button" class="btn-danger remove-category">Verwijder categorie</button></div><div class="products"></div><div class="actions"><button type="button" class="btn-secondary add-product">+ Product toevoegen</button></div>`;const products=section.querySelector('.products');if(category.products.length){category.products.forEach(product=>products.appendChild(createEditor(product)))}else{const p=document.createElement('p');p.className='note';p.textContent='Nog geen producten in deze categorie.';products.appendChild(p)}section.querySelector('.add-product').addEventListener('click',()=>{const note=products.querySelector('.note');if(note)note.remove();products.appendChild(createEditor())});section.querySelector('.remove-category').addEventListener('click',()=>section.remove());root.appendChild(section)})}document.addEventListener('DOMContentLoaded',()=>{renderAdmin();document.getElementById('addCategory').addEventListener('click',()=>{const name=prompt('Naam van nieuwe categorie');if(!name)return;adminState.categories.push({name,products:[]});renderAdmin()});document.getElementById('save').addEventListener('click',()=>{alert('Demo-opslag. Koppel dit aan jouw database of JSON-opslag.')})});
