const store={user:{name:'Evert',initials:'E'},products:[{title:'Emergency',brand:'TopOrganic',category:'Frozen Cleansift',image:'https://images.unsplash.com/photo-1506617420156-8e4536971650?auto=format&fit=crop&w=900&q=80',variants:[{amount:'10 gram',price:25},{amount:'20 gram',price:45},{amount:'50 gram',price:100}]},{title:'Grappe Gasoline',brand:'Unbrand',category:'Frozen Cleansift',image:'https://images.unsplash.com/photo-1473773508845-188df298d2d1?auto=format&fit=crop&w=900&q=80',variants:[{amount:'10 gram',price:28},{amount:'20 gram',price:50},{amount:'50 gram',price:110}]}]};function euro(v){return '€'+Number(v).toFixed(2).replace('.00','')}function renderStore(list){const grid=document.getElementById('grid');grid.innerHTML='';document.getElementById('count').textContent=`${list.length} products`;list.forEach(product=>{const first=product.variants[0];const el=document.createElement('article');el.className='card';el.innerHTML=`<img src="${product.image}" alt="${product.title}" loading="lazy"><div class="card-body"><span class="badge">${product.brand}</span><h3>${product.title}</h3><div class="variant-row">${product.variants.map((v,i)=>`<button class="variant-btn ${i===0?'active':''}" data-price="${v.price}">${v.amount}</button>`).join('')}</div><div class="price-row"><div><div class="meta">${product.category}</div><div class="price">${euro(first.price)}</div></div><button class="btn-secondary">♡</button></div></div>`;const price=el.querySelector('.price');el.querySelectorAll('.variant-btn').forEach(btn=>btn.addEventListener('click',()=>{el.querySelectorAll('.variant-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');price.textContent=euro(btn.dataset.price)}));grid.appendChild(el)})}document.addEventListener('DOMContentLoaded',()=>{document.getElementById('userName').textContent=store.user.name;document.getElementById('userInitials').textContent=store.user.initials;const cat=document.getElementById('category');[...new Set(store.products.map(p=>p.category))].forEach(v=>cat.insertAdjacentHTML('beforeend',`<option value="${v}">${v}</option>`));cat.addEventListener('change',()=>{const value=cat.value;renderStore(value?store.products.filter(p=>p.category===value):store.products)});renderStore(store.products)});
